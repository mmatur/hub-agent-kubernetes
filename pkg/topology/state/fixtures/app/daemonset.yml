apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: mydaemonset
  namespace: myns
  labels:
    one.label: value

spec:
  template:
    spec:
      containers:
        - name: foo
          image: traefik:latest

status:
  desiredNumberScheduled: 2
  numberAvailable: 1
