# Middleware in the default namespace.
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: catalog-1680030486-stripprefix
  namespace: default
spec:
  stripPrefix:
    prefixes:
      - /whoami-1
      - /whoami-2
---

# Middleware in the my-ns namespace.
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: catalog-1680030486-stripprefix
  namespace: my-ns
spec:
  stripPrefix:
    prefixes:
      - /whoami-3